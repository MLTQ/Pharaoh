rvc-python>=0.1.0
soundfile>=0.12
librosa>=0.10
# numpy must be <2.0: faiss-cpu's SWIG bindings (used by rvc-python) break with numpy 2.x.
# No lower bound — rvc-python 0.1.5 resolves to numpy 1.23.5 which is fine.
numpy<2.0
fastapi>=0.111
uvicorn>=0.29
httpx>=0.27
pydantic>=2.0
