# Pharaoh inference server dependencies

# ── Server framework (always required) ──────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
aiofiles>=24.0

# ── Real model inference (required when PHARAOH_REAL_MODELS=1) ──────────────
#
# TTS — Qwen3-TTS via dedicated package (NOT plain transformers)
#   pip install qwen-tts soundfile
#   Model dir: ~/pharaoh-models/tts   (env: PHARAOH_TTS_MODEL_DIR)
#
# SFX — Woosh runs from its own uv venv; start_servers.sh picks it up automatically
#   cd ~/Code/Woosh && uv sync              # CPU / MPS
#   cd ~/Code/Woosh && uv sync --extra cuda # NVIDIA GPU
#   Model dir: ~/Code/Woosh/checkpoints/    (env: PHARAOH_WOOSH_DIR)
#
# Music — ACE-Step
#   pip install ace-step soundfile
#   Model dir: ~/pharaoh-models/music  (env: PHARAOH_MUSIC_MODEL_DIR)
