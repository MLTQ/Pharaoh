# Pharaoh inference server dependencies

# Server framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
aiofiles>=24.0

# Real model inference (optional — only needed when PHARAOH_REAL_MODELS=1)
# transformers<5.0  # Pin: Qwen3-TTS incompatible with 5.0+
# torch>=2.3
# torchaudio>=2.3
# accelerate>=0.30

# TTS (Qwen3-TTS)
# transformers==4.51.3  # Exact version tested with Qwen3-TTS
# soundfile>=0.12

# SFX (Woosh)
# woosh-audio @ git+https://github.com/sony/audio-mae  # or pip install from release

# Music (ACE-Step)
# ace-step @ git+https://github.com/ace-step/ACE-Step

# MLX variants (Apple Silicon — for local M1 testing)
# mlx>=0.15
# mlx-lm>=0.15
