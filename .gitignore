node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.local
.env

.DS_Store

__pycache__/
inference/.venv-tts/
inference/.venv-music/

.claude/worktrees/

inference/.venv-rvc



inference/.applio

target/release/

inference/server-output/uploads/
pharaoh.mcpb

inference/server-output

# Spatial audio: SOFA HRTF files and synthesized/downloaded room IRs are
# installed by inference/download_spatial_assets.sh at runtime. Don't ship
# binary data in the repo — spaces.json + the downloader are enough.
assets/sofa/*.sofa
assets/spaces/*.wav
# Gruve agent binary (vendored from gruve-kit.zip, ~30MB — keep out of git)
gruve/gruve
gruve-kit.zip

# Remaining inference venvs (the tts/music/rvc ones are listed above)
inference/.venv-audiosr/
inference/.venv-chatterbox/
inference/.venv-applio/

# Cargo output at the repo root (src-tauri/target is listed above)
target/

# Pre-Tauri JSX prototype and the vendored Gruve kit archive
Pharoh/
Pharoh.zip
.claire/
